import { AuthShell } from '../../components/auth-shell';

export function SettingsPage() {
    return (
        <AuthShell
            title="Auth Settings"
            description="This page will render Kratos account settings flow."
        />
    );
}
